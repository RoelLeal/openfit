/**
 * Project identity in one place. "OpenFit" is a working name: check trademarks
 * before publishing (see docs/DESIGN.md §17) and rename here, in index.html,
 * public/manifest.webmanifest and package.json.
 */
export const APP_NAME = 'OpenFit';
export const APP_VERSION: string = __APP_VERSION__;
/** Link shown in "About" once the repository is public (null hides it). */
export const REPOSITORY_URL: string | null = null;
